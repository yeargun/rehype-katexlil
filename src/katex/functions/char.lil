import { undef, isUndef, isNullish, isFn, isStr, isNum, isArr, toStr, toNum, toInt, len, hasOwn, inOp, deleteKey, instanceOf, push, newArray, emptyObject, warn, getDocument, getConsole, globalDefined, globalGet, mathMin, mathMax, constructN, apply, objectAssign, objectKeys, makeError, makeTypeError, throwErr, hostParseInt, hostParseFloat, fromCharCode, Error, TypeError, Object, Array, Function, String, Number, Math, RegExp, JSON, Infinity, NaN } from "../host.lil";

import { defineFunction } from "../defineFunction.lil";
import { ParseError } from "../ParseError.lil";
import { assertNodeType } from "../parseNode.lil";

JsValue o1 = emptyObject();
o1["type"] = "textord";
JsValue a2 = newArray();
JS.push(a2, "\\@char");
o1["names"] = a2;
JsValue o3 = emptyObject();
o3["numArgs"] = 1;
o3["allowedInText"] = true;
o1["props"] = o3;
o1["handler"] = JS.method2((JsValue self, JsValue p4, JsValue args) => {
  JsValue op5 = p4;
  JsValue parser = op5["parser"];
  JsValue arg = JS.call(assertNodeType, undef(), args[0], "ordgroup");
  JsValue group = arg["body"];
  JsValue numberVal = "";
  {
    JsValue i = 0;
    while (JS.lessThan(i, group["length"])) {
      JsValue node = JS.call(assertNodeType, undef(), group[i], "textord");
      numberVal = JS.add(numberVal, node["text"]);
      JsValue u6 = i;
      i = JS.add(i, 1);
    }
  }
  JsValue code = hostParseInt(numberVal);
  JsValue text = undef();
  if (JS.invoke(Number, "isNaN", code).truthy()) {
    throw JS.construct(ParseError, "\\@char has non-numeric argument " + toStr(numberVal));
  } else {
    if ((JS.lessThan(code, 0)) || (JS.greaterThanOrEqual(code, 1114111))) {
      throw JS.construct(ParseError, "\\@char with invalid code point " + toStr(numberVal));
    } else {
      if (JS.lessThanOrEqual(code, 65535)) {
        text = JS.invoke(String, "fromCharCode", code);
      } else {
        code = (toNum(code) - toNum(65536));
        text = JS.invoke(String, "fromCharCode", JS.add((toInt(code) >> toInt(10)), 55296), JS.add((toInt(code) & toInt(1023)), 56320));
      }
    }
  }
  JsValue o7 = emptyObject();
  o7["type"] = "textord";
  o7["mode"] = parser["mode"];
  o7["text"] = text;
  return o7;
  return undef();
});
JS.call(defineFunction, undef(), o1);
JsValue __star = emptyObject();

export { __star };
